[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "my_backend:builder"
backend-path = ["backend_dir"]

[project]
name = "poc-backend-path"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []
description = "dummy"
